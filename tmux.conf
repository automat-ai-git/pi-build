# 256 цветов
set -g default-terminal "tmux-256color"

# Расширенные клавиши для ttyd/браузера (требует tmux 3.5+)
set -g extended-keys on
set -g extended-keys-format csi-u

# Побольше истории
set -g history-limit 50000

# Мышь — скролл и выделение
set -g mouse on

# Копирование в буфер обмена браузера через OSC52 (ttyd поддерживает)
set -g set-clipboard on

# Нумерация окон с 1
set -g base-index 1
setw -g pane-base-index 1

# Статус-бар: понятно где мы
set -g status-style "bg=colour235,fg=colour248"
set -g status-left " π #S "
set -g status-right " #H | %H:%M "

# Автоматически перенумеровывать окна при закрытии
set -g renumber-windows on
